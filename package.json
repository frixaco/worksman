{
  "name": "worksman",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "bun build *.ts --outdir . --watch",
    "build:chrome": "bun build chrome/*.ts --outdir chrome/",
    "build:firefox": "bun build firefox/*.ts --outdir firefox/"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.313",
    "@types/firefox-webext-browser": "^120.0.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {}
}
